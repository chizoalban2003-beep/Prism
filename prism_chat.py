def get_chat_html() -> str:
    return """<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>PRISM</title>
<style>
:root{--bg:#0f1117;--sf:#171b22;--br:rgba(255,255,255,.08);--tx:#ecf0f6;--mu:#9aa4b2;--ac:#5DCAA5;--rd:#E24B4A;--ye:#EF9F27;--bl:#3B8BD4}
@media (prefers-color-scheme: light){:root{--bg:#f5f7fb;--sf:#ffffff;--br:rgba(15,23,42,.12);--tx:#162030;--mu:#5b6778}}
*{box-sizing:border-box}html,body{height:100%}body{margin:0;background:var(--bg);color:var(--tx);font:14px/1.45 system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}
.app{display:grid;grid-template-columns:220px minmax(0,1fr);min-height:100vh}.sidebar{border-right:1px solid var(--br);background:var(--sf);padding:18px;display:flex;flex-direction:column;gap:18px}.brand{font-weight:800;letter-spacing:.1em}.group-title{font-size:11px;color:var(--mu);text-transform:uppercase;letter-spacing:.08em;margin:0 0 8px}.nav button,.identity button,.quick button,.send{appearance:none;border:1px solid var(--br);background:transparent;color:var(--tx);border-radius:12px;padding:10px 12px;text-align:left;cursor:pointer}.nav,.identity,.quick{display:grid;gap:8px}.status{margin-top:auto;padding:12px;border:1px solid var(--br);border-radius:14px;color:var(--mu)}.main{display:flex;flex-direction:column;min-width:0}.header{padding:18px 20px;border-bottom:1px solid var(--br);background:var(--sf);display:flex;justify-content:space-between;gap:12px;align-items:center}.messages{flex:1;overflow:auto;padding:20px;display:flex;flex-direction:column;gap:14px}.msg{max-width:860px}.msg.user{align-self:flex-end}.bubble,.card{border:1px solid var(--br);background:var(--sf);border-radius:16px;padding:14px 16px}.msg.user .bubble{background:rgba(59,139,212,.14)}.label{font-size:11px;color:var(--mu);margin:0 0 6px 4px}.card h3{margin:0 0 8px;font-size:15px}.sub{color:var(--mu)}.row{display:flex;gap:10px;align-items:center}.row+.row,.stack+.stack{margin-top:10px}.time{width:72px;color:var(--mu)}.bar{height:10px;border-radius:999px;background:rgba(255,255,255,.06);border:1px solid var(--br);overflow:hidden;flex:1}.fill{height:100%;background:linear-gradient(90deg,var(--ac),var(--bl))}.fill.warn{background:linear-gradient(90deg,var(--ye),var(--rd))}.pill{display:inline-block;padding:4px 8px;border-radius:999px;border:1px solid var(--br);font-size:11px;color:var(--mu);margin:8px 8px 0 0}.composer{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;padding:16px 20px;border-top:1px solid var(--br);background:var(--sf)}textarea{width:100%;min-height:64px;max-height:180px;resize:vertical;border-radius:14px;border:1px solid var(--br);background:transparent;color:var(--tx);padding:12px 14px;font:inherit}.send{background:var(--ac);color:#082117;font-weight:700;padding:0 18px}.quick{padding:0 20px 16px}.muted{color:var(--mu)}
.tts-btn{appearance:none;border:1px solid var(--br);background:transparent;color:var(--mu);border-radius:10px;padding:6px 12px;cursor:pointer;font-size:12px;transition:color .15s,border-color .15s}.tts-btn.on{color:var(--ac);border-color:var(--ac)}
.nav button,.identity button{transition:background .15s,color .15s,border-color .15s}.nav button:hover,.identity button:hover{border-color:var(--ac);color:var(--ac)}.nav button.active,.identity button.active{background:var(--ac);color:#082117;border-color:var(--ac);font-weight:700}
.notif-banner{display:none;padding:10px 20px;background:rgba(239,159,39,.1);border-bottom:1px solid rgba(239,159,39,.3);font-size:12px;color:var(--ye);gap:10px;align-items:flex-start;flex-direction:column}.notif-banner.visible{display:flex}.notif-item{display:flex;gap:8px;align-items:center;width:100%}.notif-item button{appearance:none;border:none;background:transparent;color:var(--mu);cursor:pointer;font-size:11px;margin-left:auto;padding:2px 6px}
.token-bar{display:none;padding:14px 20px;background:rgba(93,202,165,.08);border-bottom:1px solid rgba(93,202,165,.3);flex-direction:column;gap:10px}.token-bar.visible{display:flex}.token-msg{font-size:12px;color:var(--tx)}.token-msg code{background:rgba(0,0,0,.3);padding:2px 6px;border-radius:6px;font-size:11px;color:var(--ac)}.token-row{display:flex;gap:8px}.token-row input{flex:1;border-radius:10px;border:1px solid var(--br);background:transparent;color:var(--tx);padding:9px 12px;font:inherit}.token-row button{appearance:none;border:none;border-radius:10px;background:var(--ac);color:#082117;font-weight:700;padding:0 18px;cursor:pointer}.token-err{display:none;font-size:11px;color:var(--rd)}.token-err.visible{display:block}
@media (max-width:580px){.app{grid-template-columns:1fr}.sidebar{display:none}.composer{grid-template-columns:1fr}.messages,.header,.composer,.quick{padding-left:14px;padding-right:14px}}
</style>
</head>
<body>
<div class="app">
  <aside class="sidebar">
    <div class="brand">PRISM</div>
    <div>
      <p class="group-title">Modules</p>
      <div class="nav">
        <button data-mode="General">General</button>
        <button data-mode="Daily">Daily tasks</button>
        <button data-mode="Medical">Medical</button>
        <button data-mode="Financial">Financial</button>
        <button data-mode="Legal">Legal</button>
        <button data-mode="Sport">Sport</button>
        <button data-mode="Developer">Developer</button>
      </div>
    </div>
    <div>
      <p class="group-title">Identity</p>
      <div class="identity">
        <button data-demo="identity profile">Profile</button>
        <button data-demo="artifacts">Artifacts</button>
      </div>
    </div>
    <div class="status">Status: <span id="statusText">checking\u2026</span></div>
    <div id="perception-status" style="margin-top:6px;font-size:9.5px;color:var(--mu);padding:0 .3rem"></div>
  </aside>
  <main class="main">
    <div class="header">
      <div><strong>PRISM Chat</strong><div class="sub" id="modeBadge">General mode</div></div>
      <div style="display:flex;gap:10px;align-items:center">
        <button id="ttsBtn" class="tts-btn" title="Toggle text-to-speech">\ud83d\udd0a TTS</button>
        <div id="roleText" class="sub">offline</div>
      </div>
    </div>
    <div id="tokenBar" class="token-bar">
      <div class="token-msg">🔒 PRISM is locked. Paste your auth token to connect — find it with <code>cat ~/.prism/auth_token</code></div>
      <div class="token-row">
        <input id="tokenInput" type="password" placeholder="Auth token" autocomplete="off" spellcheck="false">
        <button id="tokenBtn" type="button" onclick="connectToken()">Connect</button>
      </div>
      <div id="tokenErr" class="token-err"></div>
    </div>
    <div id="notifBanner" class="notif-banner"></div>
    <div id="messages" class="messages"></div>
    <div class="quick" id="quick"></div>
    <form class="composer" id="composer">
      <textarea id="message" placeholder="Ask PRISM anything\u2026"></textarea>
      <button class="send" type="submit">Send</button>
    </form>
  </main>
</div>
<script>
const API='';
const state={mode:'General',ttsOn:false};
const messages=document.getElementById('messages');
const quick=document.getElementById('quick');
const input=document.getElementById('message');
const statusText=document.getElementById('statusText');
const roleText=document.getElementById('roleText');
const ttsBtn=document.getElementById('ttsBtn');
const notifBanner=document.getElementById('notifBanner');
const quickPrompts=['Plan my day','Help me plan: run a marathon in 6 months','How do I get my first 100 customers?','Predict City vs Arsenal','Show squad risk','Medical triage','Portfolio invest','Identity profile','Log wearables: HRV 58ms, sleep 7hrs',"What's my current context?"];
const MODE_CHIPS={General:["What can you do?","Plan my day","Help me reach a goal","What's my current context?","Log a decision"],Daily:["What's on my calendar today?","Check my emails","List files in my Downloads","Plan my day","Turn off the lights"],Medical:['Medical triage','Assess injury risk','Recovery protocol','HRV analysis','Squad health overview'],Financial:['Portfolio invest','Risk assessment','Market analysis','Budget planning','Investment strategy'],Legal:['Legal case strategy','Contract review','Compliance check','Risk assessment','Case outcome prediction'],Sport:['Predict City vs Arsenal','Show squad risk','Duel network analysis','Moment prediction','Training load'],Developer:['Show system status','Run diagnostics','Search in files','Git status','Install package']};
function esc(value){return String(value==null?'':value).replace(/[&<>"]/g,function(ch){return({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'})[ch];});}
function pct(value){const n=Number(value);if(!Number.isFinite(n))return 0;return Math.max(0,Math.min(100,Math.round((n<=1?n*100:n))));}
function bubble(role,html){const wrap=document.createElement('div');wrap.className='msg '+role;wrap.innerHTML='<div class="label">'+(role==='user'?'You':'PRISM')+'</div>'+html;messages.appendChild(wrap);messages.scrollTop=messages.scrollHeight;}
function renderCard(card){card=card||{type:'text',body:''};const data=card.data||{};if(card.type==='plan'&&!data.strategies){const pid='plan_'+Math.random().toString(36).slice(2,8);const rows=(data.tasks||[]).map(function(task,i){const tid=pid+'_t'+i;return '<div class="row" data-row="'+tid+'"><div class="time"><input data-pf="time" type="text" value="'+esc(task.time||'')+'" disabled style="background:transparent;border:none;color:var(--mu);font:11px inherit;width:64px;padding:0"></div><div style="flex:1"><input data-pf="title" type="text" value="'+esc(task.title||task.category||'Task')+'" disabled style="background:transparent;border:none;color:var(--tx);font:13px inherit;width:100%;padding:0"></div></div>';}).join('');return '<div class="card" id="'+pid+'"><h3>'+esc(card.title||'Daily Plan')+'</h3><div data-rows>'+rows+'</div><div class="pill">'+esc(data.primary_focus||'')+'</div><textarea id="'+pid+'_instr" placeholder="Refinements for re-plan (e.g. \\'move workout to morning\\', \\'remove the meeting\\', \\'add 30min of reading\\')" style="width:100%;min-height:50px;margin:10px 0 6px;padding:8px 10px;border-radius:8px;background:rgba(0,0,0,.25);border:1px solid var(--br);color:var(--tx);font:11px/1.45 inherit;resize:vertical;box-sizing:border-box"></textarea><div style="display:flex;gap:8px;margin-top:6px"><button onclick="togglePlanEdit(\\''+pid+'\\',this)" style="flex:1;padding:6px;border-radius:6px;background:var(--sf);border:1px solid var(--br);cursor:pointer;font-size:11px;color:var(--tx);font-family:inherit">Edit</button><button onclick="replanPlan(\\''+pid+'\\',this)" style="flex:1;padding:6px;border-radius:6px;background:var(--ac);border:none;cursor:pointer;font-size:11px;color:#082117;font-weight:700;font-family:inherit">Re-plan</button></div></div>';}if(card.type==='prediction'){return '<div class="card"><h3>'+esc(card.title||'Match Prediction')+'</h3><div class="stack"><div>'+esc(data.home||'Home')+' vs '+esc(data.away||'Away')+'</div><div class="row"><span>Home</span><div class="bar"><div class="fill" style="width:'+pct(data.p_home)+'%"></div></div><strong>'+pct(data.p_home)+'%</strong></div><div class="row"><span>Draw</span><div class="bar"><div class="fill" style="width:'+pct(data.p_draw)+'%"></div></div><strong>'+pct(data.p_draw)+'%</strong></div><div class="row"><span>Away</span><div class="bar"><div class="fill warn" style="width:'+pct(data.p_away)+'%"></div></div><strong>'+pct(data.p_away)+'%</strong></div></div></div>';}if(card.type==='squad'){return '<div class="card"><h3>'+esc(card.title||'Squad Risk Overview')+'</h3>'+(data.players||[]).map(function(player){return '<div class="row"><span>'+esc(player.name)+'</span><div class="bar"><div class="fill warn" style="width:'+pct(player.confidence)+'%"></div></div><span>'+esc(player.risk_level)+'</span></div>';}).join('')+'</div>';}if(card.type==='domain'){return '<div class="card"><h3>'+esc(card.title||'Domain Recommendation')+'</h3><div class="sub">'+esc(data.recommended||'')+'</div>'+(data.options||[]).map(function(option){return '<div class="row"><span>'+esc(option.name)+'</span><div class="bar"><div class="fill" style="width:'+pct(option.activation)+'%"></div></div><strong>'+pct(option.activation)+'%</strong></div>';}).join('')+'</div>';}if(card.type==='identity'){return '<div class="card"><h3>'+esc(card.title||'Your Decision Profile')+'</h3>'+(data.domains||[]).map(function(domain){return '<div class="row"><span>'+esc(domain.label)+'</span><div class="bar"><div class="fill" style="width:'+pct(domain.value)+'%"></div></div><span>'+esc(domain.crystallised?'set':'emerging')+'</span></div>';}).join('')+'<div class="sub">'+esc(data.insight||'')+'</div></div>';}if(card.type==='plan'&&data.strategies){const strats=data.strategies||[];const top=strats[0]||{};const bd='<div class="label">PRISM</div>';const stepRows=(top.steps||[]).slice(0,4).map(function(s){return '<div class="si"><div class="st">'+esc(s.timeline||'')+'</div><div class="sd" style="background:var(--ac)"></div><div class="stx">'+esc(s.action)+'</div></div>';}).join('');const altRows=strats.slice(1,4).map(function(s,i){return '<div class="br"><div class="bl">Alt '+(i+1)+': '+esc(s.name)+'</div><div class="bt"><div class="bf" style="width:'+(s.activation*100).toFixed(0)+'%;background:var(--mu);opacity:.6"></div></div><div class="bp">'+(s.activation*100).toFixed(0)+'%</div></div>';}).join('');return bd+'<div class="card"><div class="card-t">\u2605 '+esc(top.name||'Optimal strategy')+' <span style="font-size:10px;opacity:.5">('+((top.activation||0)*100).toFixed(0)+'%)</span></div>'+stepRows+altRows+'<div class="meta">Domain <b>'+esc(data.domain||'')+'</b> \u00b7 Timeline <b>'+esc(data.timeline||'')+'</b> \u00b7 Fulcrum <b>'+(data.fulcrum||0).toFixed(3)+'</b></div></div>';}if(card.type==='approval'){const d=card.data||{};const bd='<div class="label">PRISM</div>';const isSynth=d.kind==='synthesis';const hasRichBody=isSynth||/<\\w+[^>]*>/.test(String(card.body||''));const taid='instr_'+(d.task_id||Math.random().toString(36).slice(2,8));const bodyHtml=hasRichBody?String(card.body||''):esc(card.body||'');const noRefine=d.no_refine===true;const placeholderTxt=isSynth?'Optional: any specific instructions, constraints, or preferences for how I should build this?':'Optional: any refinements, constraints, or instructions to attach to this action.';const textarea=noRefine?'':'<textarea id="'+taid+'" placeholder="'+placeholderTxt+'" style="width:100%;min-height:50px;margin:10px 0 6px;padding:8px 10px;border-radius:8px;background:rgba(0,0,0,.25);border:1px solid var(--br);color:var(--tx);font:11px/1.45 inherit;resize:vertical;box-sizing:border-box"></textarea>';const approveArgs=esc(JSON.stringify(d))+',\\'approve\\''+(noRefine?'':',\\''+taid+'\\'');const denyArgs=esc(JSON.stringify(d))+',\\'deny\\''+(noRefine?'':',\\''+taid+'\\'');return bd+'<div class="card" style="border-color:var(--ye)"><div class="card-t" style="color:var(--ye)">\u26a0 '+esc(card.title)+'</div><div style="font-size:11px;margin-bottom:.25rem;line-height:1.5">'+bodyHtml+'</div>'+textarea+'<div style="display:flex;gap:8px;margin-top:8px"><button onclick="approveTask('+approveArgs+',this)" style="flex:1;padding:6px;border-radius:6px;background:var(--ac);border:none;cursor:pointer;font-size:11px;color:#082117;font-weight:700;font-family:inherit">Approve</button><button onclick="approveTask('+denyArgs+',this)" style="flex:1;padding:6px;border-radius:6px;background:var(--sf);border:.5px solid var(--br);cursor:pointer;font-size:11px;color:var(--tx);font-family:inherit">Deny</button></div></div>';}if(data.kind==='ceremony'){const cid='cer_'+Math.random().toString(36).slice(2,8);const mode=data.mode||'invite';if(mode==='invite'){return '<div class="card" style="border-color:var(--ac)"><h3>\ud83c\udf31 '+esc(card.title||'Welcome')+'</h3><div style="font-size:12px;line-height:1.55;color:var(--mu);margin:4px 0 12px">PRISM becomes <em>your</em> Prism through a short conversation \u2014 '+(data.total_questions||7)+' questions, about three minutes. Your answers stay on this device and shape every decision I make for you.</div><button id="'+cid+'_btn" onclick="startCeremony(this)" style="width:100%;padding:9px;border-radius:6px;background:var(--ac);border:none;cursor:pointer;font-size:12px;color:#082117;font-weight:700;font-family:inherit">Start the ceremony</button></div>';}if(mode==='question'){const qi=data.question_index||0;const tot=data.total_questions||7;const pct=Math.round((qi/tot)*100);return '<div class="card" style="border-color:var(--ac)"><div style="font-size:10px;color:var(--mu);margin-bottom:4px">Question '+(qi+1)+' of '+tot+'</div><div style="height:3px;background:rgba(255,255,255,.06);border-radius:2px;margin:0 0 12px"><div style="height:100%;background:var(--ac);border-radius:2px;width:'+pct+'%"></div></div><div style="font-size:13px;font-weight:600;line-height:1.45;margin:0 0 10px">'+esc(data.question||'')+'</div><textarea id="'+cid+'_ans" placeholder="Type your answer\u2026" style="width:100%;min-height:80px;padding:9px;border-radius:6px;background:rgba(0,0,0,.25);border:1px solid var(--br);color:var(--tx);font:12px/1.45 inherit;resize:vertical;box-sizing:border-box"></textarea><div style="display:flex;gap:8px;margin-top:8px"><button onclick="ceremonyAnswer(\\''+cid+'\\',false,this)" style="flex:1;padding:7px;border-radius:6px;background:var(--ac);border:none;cursor:pointer;font-size:11px;color:#082117;font-weight:700;font-family:inherit">Next \u2192</button><button onclick="ceremonyAnswer(\\''+cid+'\\',true,this)" style="padding:7px 12px;border-radius:6px;background:var(--sf);border:1px solid var(--br);cursor:pointer;font-size:11px;color:var(--mu);font-family:inherit">Skip</button></div></div>';}if(mode==='complete'){const seed=data.seed||{};const pills=(seed.stated_values||[]).slice(0,6).map(function(v){return '<span class="pill" style="border-color:var(--ac);color:var(--ac)">'+esc(v)+'</span>';}).join('');const goals=(seed.stated_goals||[]).slice(0,3).map(function(g){return '<div style="font-size:11px;color:var(--mu);margin:2px 0">\u2022 '+esc(g)+'</div>';}).join('');return '<div class="card" style="border-color:var(--ac)"><h3>\u2728 Your PRISM is ready</h3><div style="font-size:12px;color:var(--mu);margin:6px 0 10px">I\\'ve set up a starting model based on what you told me. I\\'ll keep learning every time we talk.</div>'+(pills?'<div style="margin:6px 0">'+pills+'</div>':'')+goals+'</div>';}return '<div class="card"><h3>'+esc(card.title||'PRISM')+'</h3></div>';}if(data.kind==='setup_form'){const sec=esc(data.section||'');const formId='setup_'+sec+'_'+Math.random().toString(36).slice(2,8);const fields=(data.fields||[]).map(function(f){const id=formId+'_'+esc(f.name);const cv=(data.current_values||{})[f.name];const isSet=cv==='__set__';const val=isSet?'':(cv==null?(f.default==null?'':String(f.default)):String(cv));const placeholder=esc(f.placeholder||(isSet?'\u2022\u2022\u2022\u2022\u2022\u2022 (already set; leave blank to keep)':''));const lbl='<label style="display:block;font-size:11px;color:var(--mu);margin:8px 0 3px">'+esc(f.label||f.name)+(f.required?' <span style=color:var(--rd)>*</span>':'')+'</label>';let inp='';if(f.kind==='select'){const opts=(f.choices||[]).map(function(c){return '<option value="'+esc(c)+'"'+(String(val)===String(c)?' selected':'')+'>'+esc(c||'(none)')+'</option>';}).join('');inp='<select id="'+id+'" name="'+id+'" data-field="'+esc(f.name)+'" data-kind="select" autocomplete="off" style="width:100%;padding:7px 9px;border-radius:6px;background:rgba(0,0,0,.25);border:1px solid var(--br);color:var(--tx);font:12px inherit">'+opts+'</select>';}else{const type=f.kind==='password'?'password':(f.kind==='number'?'number':(f.kind==='url'?'url':'text'));const ac=f.kind==='password'?'new-password':'off';inp='<input id="'+id+'" name="'+id+'" data-field="'+esc(f.name)+'" data-kind="'+esc(f.kind||'text')+'" type="'+type+'" value="'+esc(val)+'" placeholder="'+placeholder+'" autocomplete="'+ac+'" autocorrect="off" autocapitalize="off" spellcheck="false" style="width:100%;padding:7px 9px;border-radius:6px;background:rgba(0,0,0,.25);border:1px solid var(--br);color:var(--tx);font:12px inherit;box-sizing:border-box">';}return lbl+inp;}).join('');const why=data.why?'<div style="font-size:11px;color:var(--mu);line-height:1.5;margin:0 0 6px">'+esc(data.why)+'</div>':'';const docs=data.docs_url?'<div style="margin:10px 0 0;font-size:11px"><a href="'+esc(data.docs_url)+'" target="_blank" rel="noopener" style="color:var(--ac);text-decoration:none">Open setup docs \u2197</a></div>':'';return '<div class="card" style="border-color:var(--bl)"><h3>\u2699 '+esc(card.title||'Setup')+'</h3>'+why+'<div id="'+formId+'">'+fields+'</div><div style="display:flex;gap:8px;margin-top:12px"><button onclick="saveSettings(\\''+sec+'\\',\\''+formId+'\\',this)" style="flex:1;padding:7px;border-radius:6px;background:var(--ac);border:none;cursor:pointer;font-size:11px;color:#082117;font-weight:700;font-family:inherit">Save</button></div>'+docs+'</div>';}return '<div class="card"><h3>'+esc(card.title||'PRISM')+'</h3><div>'+String(card.body||'')+'</div></div>';}
async function startCeremony(btn){if(btn){btn.disabled=true;btn.textContent='Starting\u2026';}try{const r=await fetch(API+'/onboarding/start',{method:'POST',signal:AbortSignal.timeout(8000)});const d=await r.json();const card={type:'text',title:'',body:'',data:{kind:'ceremony',mode:'question',question:d.question||'',question_index:d.question_index||0,total_questions:d.total_questions||7}};const msgs=document.querySelectorAll('.msg');if(msgs.length)msgs[msgs.length-1].remove();bubble('agent',renderCard(card));}catch(e){if(btn){btn.disabled=false;btn.textContent='Start the ceremony';}bubble('agent',renderCard({type:'text',title:'Ceremony',body:'Could not start ceremony.',data:{}}));}}
async function ceremonyAnswer(cid,skipped,btn){const ta=document.getElementById(cid+'_ans');const answer=skipped?'\u2014':(ta?ta.value.trim():'');if(!skipped&&!answer){if(ta)ta.focus();return;}if(btn){btn.disabled=true;btn.textContent='Saving\u2026';}try{const r=await fetch(API+'/onboarding/answer',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({answer:answer||'\u2014'}),signal:AbortSignal.timeout(30000)});const d=await r.json();const msgs=document.querySelectorAll('.msg');if(msgs.length)msgs[msgs.length-1].remove();if(d.complete){bubble('agent',renderCard({type:'text',title:'',body:'',data:{kind:'ceremony',mode:'complete',seed:d.seed||{}}}));}else{bubble('agent',renderCard({type:'text',title:'',body:'',data:{kind:'ceremony',mode:'question',question:d.question||'',question_index:d.question_index||0,total_questions:d.total_questions||7}}));}}catch(e){if(btn){btn.disabled=false;btn.textContent='Next \u2192';}bubble('agent',renderCard({type:'text',title:'Ceremony',body:'Could not submit answer.',data:{}}));}}
async function saveSettings(section,formId,btn){const wrap=document.getElementById(formId);if(!wrap)return;const inputs=wrap.querySelectorAll('input,select');const values={};inputs.forEach(function(el){const k=el.getAttribute('data-field');if(!k)return;values[k]=el.value;});if(btn){btn.disabled=true;btn.textContent='Saving\u2026';}try{const r=await fetch(API+'/settings/save',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({section:section,values:values}),signal:AbortSignal.timeout(15000)});const d=await r.json();if(!r.ok){bubble('agent',renderCard({type:'text',title:section+' \u2014 error',body:esc(d.error||'Save failed'),data:{}}));}else{bubble('agent',renderCard(d.card||{type:'text',title:'Saved',body:'Settings saved.',data:{}}));}}catch(e){bubble('agent',renderCard({type:'text',title:section+' \u2014 error',body:'Could not save settings.',data:{}}));}if(btn){btn.disabled=false;btn.textContent='Save';}}
function togglePlanEdit(pid,btn){const card=document.getElementById(pid);if(!card)return;const inputs=card.querySelectorAll('input[data-pf]');const editing=btn.getAttribute('data-edit')==='1';inputs.forEach(function(el){el.disabled=editing;if(!editing){el.style.borderBottom='1px dashed var(--br)';}else{el.style.borderBottom='none';}});btn.setAttribute('data-edit',editing?'0':'1');btn.textContent=editing?'Edit':'Done';}
async function replanPlan(pid,btn){const card=document.getElementById(pid);if(!card)return;const instr=(document.getElementById(pid+'_instr')||{}).value||'';const tasks=[];card.querySelectorAll('[data-rows] .row').forEach(function(row){const t=row.querySelector('input[data-pf="time"]');const ti=row.querySelector('input[data-pf="title"]');tasks.push({time:t?t.value:'',title:ti?ti.value:''});});if(btn){btn.disabled=true;btn.textContent='Re-planning\u2026';}try{const r=await fetch(API+'/plan/replan',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({instructions:instr,tasks:tasks}),signal:AbortSignal.timeout(45000)});const d=await r.json();bubble('agent',renderCard(d));}catch(e){bubble('agent',renderCard({type:'text',title:'Re-plan error',body:'Could not re-plan.',data:{}}));}if(btn){btn.disabled=false;btn.textContent='Re-plan';}}
async function approveTask(data,decision,instrId,btn){const d=data||{};if(btn){if(btn.disabled)return;const parent=btn.closest('.card')||btn.parentNode;if(parent)parent.querySelectorAll('button').forEach(function(b){b.disabled=true;b.style.opacity='0.5';b.style.cursor='not-allowed';});if(btn.tagName==='BUTTON')btn.textContent=decision==='approve'?'Approving\u2026':'Denying\u2026';}let instructions='';if(instrId){const t=document.getElementById(instrId);if(t&&t.value)instructions=t.value;}const verb=decision==='approve'?'Approved':'Denied';const userLabel=instructions?verb+' with instructions':verb;bubble('user','<div class="bubble">'+esc(userLabel)+(instructions?'<div style="margin-top:4px;font-size:11px;opacity:.75">"'+esc(instructions.slice(0,140))+(instructions.length>140?'\u2026':'')+'"</div>':'')+'</div>');bubble('agent','<div class="card"><div class="label">PRISM</div><span class="muted">Processing\u2026</span></div>');let card;try{const r=await fetch(API+'/device/approve',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({task:d.task||'',params:d.params||{},instructions:instructions,approved:decision==='approve'}),signal:AbortSignal.timeout(60000)});if(r.ok)card=await r.json();else throw new Error();}catch{card={type:'text',title:'',body:'Could not process approval.',data:{},actions:[]};}const msgs=document.querySelectorAll('.msg');if(msgs.length)msgs[msgs.length-1].remove();bubble('agent',renderCard(card));}
function demo(msg){const text=String(msg||'').toLowerCase();if(/plan|day|morning/.test(text)){return {type:'plan',title:'Daily Plan',body:'',data:{primary_focus:'Recovery',tasks:[{time:'07:00',category:'prep',title:'Mobility primer',duration:20},{time:'09:00',category:'focus',title:'Deep work block',duration:90},{time:'13:00',category:'review',title:'Film review',duration:45},{time:'17:00',category:'reset',title:'Recovery walk',duration:30}]}};}if(/predict|match|vs/.test(text)){return {type:'prediction',title:'Match Prediction',body:'',data:{home:'City',away:'Arsenal',p_home:0.46,p_draw:0.26,p_away:0.28,predicted:'City',confidence:0.68}};}if(/injury|risk|squad/.test(text)){return {type:'squad',title:'Squad Risk Overview',body:'',data:{players:[{name:'Player A',risk_level:'high',confidence:0.82},{name:'Player B',risk_level:'medium',confidence:0.61},{name:'Player C',risk_level:'low',confidence:0.24}]}};}if(/identity|profile/.test(text)){return {type:'identity',title:'Your Decision Profile',body:'',data:{domains:[{label:'Medical',value:0.74,crystallised:true},{label:'Financial',value:0.43,crystallised:true},{label:'Sport',value:0.58,crystallised:false},{label:'Developer',value:0.36,crystallised:false}],insight:'Pattern still crystallising',confidence:0.63,n_decisions:12}};}if(/triage|medical/.test(text)){return {type:'domain',title:'Medical Recommendation',body:'',data:{domain:'Medical',recommended:'Urgent_GP',confidence:0.78,fulcrum:0.61,options:[{name:'GP',activation:0.42},{name:'Urgent_GP',activation:0.78},{name:'AE_4hr',activation:0.57}]}};}if(/portfolio|invest/.test(text)){return {type:'domain',title:'Financial Recommendation',body:'',data:{domain:'Financial',recommended:'Mod_balanced',confidence:0.74,fulcrum:0.49,options:[{name:'Gov_bonds',activation:0.31},{name:'Mod_balanced',activation:0.74},{name:'Growth',activation:0.52}]}};}return {type:'text',title:'PRISM',body:'Try: plan my day, predict City vs Arsenal, squad risk, identity profile, medical triage, or portfolio invest.',data:{}};}
let _sendInFlight=false;
async function send(override){const message=(override!=null?override:input.value).trim();if(!message)return;if(_sendInFlight)return;_sendInFlight=true;const sendBtn=document.querySelector('.send');if(sendBtn){sendBtn.disabled=true;sendBtn.style.opacity='0.5';}function _release(){_sendInFlight=false;if(sendBtn){sendBtn.disabled=false;sendBtn.style.opacity='';}}bubble('user','<div class="bubble">'+esc(message)+'</div>');input.value='';const wrap=document.createElement('div');wrap.className='msg agent';const streamId='sb'+Date.now();wrap.innerHTML='<div class="label">PRISM</div><div class="card" id="'+streamId+'"><span class="muted">Thinking\u2026</span></div>';messages.appendChild(wrap);messages.scrollTop=messages.scrollHeight;function _render(d){const ct=(d.card_type||'text').replace(/^cardtype\\./i,'');const card={type:ct,title:d.card_title||'PRISM',body:d.answer||'',data:d.card_data||{},actions:d.card_actions||[]};wrap.innerHTML='<div class="label">PRISM</div>'+renderCard(card);messages.scrollTop=messages.scrollHeight;}async function fallback(){try{const r=await fetch(API+'/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:message,mode:state.mode})});if(!r.ok)throw new Error('offline');const c=await r.json();wrap.innerHTML='<div class="label">PRISM</div>'+renderCard(c);}catch(e){wrap.innerHTML='<div class="label">PRISM</div>'+renderCard(demo(message));}messages.scrollTop=messages.scrollHeight;_release();}if(typeof EventSource==='undefined'){fallback();return;}try{const src=new EventSource(API+'/stream/chat?message='+encodeURIComponent(message));let stepLog='',gotDone=false,timer=null,hardCap=null;const INACTIVITY_MS=60000,HARD_CAP_MS=600000;function _armInactivity(){if(timer)clearTimeout(timer);timer=setTimeout(function(){src.close();if(!gotDone)fallback();},INACTIVITY_MS);}_armInactivity();hardCap=setTimeout(function(){if(!gotDone){src.close();fallback();}},HARD_CAP_MS);src.onmessage=function(e){_armInactivity();try{const d=JSON.parse(e.data);if(d.event==='step'){stepLog+='<div style="font-size:10px;color:var(--mu);margin:2px 0">\u2192 '+esc(d.logic)+(d.score?' \u00b7 '+d.score+'/5':'')+'</div>';const el=document.getElementById(streamId);if(el){el.innerHTML=stepLog+'<span class="muted">Processing\u2026</span>';}messages.scrollTop=messages.scrollHeight;}else if(d.event==='done'){gotDone=true;if(timer)clearTimeout(timer);if(hardCap)clearTimeout(hardCap);src.close();_render(d);_release();}else if(d.event==='error'){gotDone=true;if(timer)clearTimeout(timer);if(hardCap)clearTimeout(hardCap);src.close();wrap.innerHTML='<div class="label">PRISM</div><div class="card"><span class="muted">'+esc(d.message||'Error')+'</span></div>';messages.scrollTop=messages.scrollHeight;_release();}}catch(err){}};src.onerror=function(){if(!gotDone){if(timer)clearTimeout(timer);if(hardCap)clearTimeout(hardCap);src.close();fallback();}};}catch(e){fallback();}}
function renderQuick(){const prompts=MODE_CHIPS[state.mode]||quickPrompts;quick.innerHTML=prompts.map(function(prompt){return '<button type="button" data-prompt="'+esc(prompt)+'">'+esc(prompt)+'</button>';}).join('');}
async function toggleTTS(){try{const r=await fetch(API+'/tts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'toggle'}),signal:AbortSignal.timeout(2000)});if(r.ok){const d=await r.json();state.ttsOn=d.enabled;}}catch{state.ttsOn=!state.ttsOn;}ttsBtn.classList.toggle('on',state.ttsOn);ttsBtn.title=state.ttsOn?'TTS on \u2014 click to disable':'TTS off \u2014 click to enable';}
function showTokenBar(){document.getElementById('tokenBar').classList.add('visible');statusText.textContent='auth required';roleText.textContent='locked';const inp=document.getElementById('tokenInput');inp.focus();if(!inp._bound){inp._bound=true;inp.addEventListener('keydown',function(ev){if(ev.key==='Enter'){ev.preventDefault();connectToken();}});}}
async function connectToken(){const inp=document.getElementById('tokenInput');const btn=document.getElementById('tokenBtn');const err=document.getElementById('tokenErr');const tok=(inp.value||'').trim();if(!tok){inp.focus();return;}btn.disabled=true;btn.textContent='Connecting…';err.classList.remove('visible');try{const r=await fetch(API+'/status?token='+encodeURIComponent(tok),{signal:AbortSignal.timeout(5000)});if(r.ok){const d=await r.json();document.getElementById('tokenBar').classList.remove('visible');inp.value='';const ollamaStr=d.ollama?('Ollama: '+(d.ollama_model||'running')+' ✓'):'Ollama: not running';statusText.textContent='connected · '+ollamaStr;roleText.textContent=d.role||'connected';pollProactive();bubble('agent',renderCard({type:'text',title:'PRISM',body:'Connected. This browser will stay signed in on this device.',data:{}}));}else if(r.status===401){err.textContent='That token was rejected — check for stray whitespace and try again.';err.classList.add('visible');inp.select();}else{err.textContent='Unexpected response ('+r.status+') — check the daemon log.';err.classList.add('visible');}}catch(e){err.textContent='Could not reach the daemon — is it running on 127.0.0.1:8742?';err.classList.add('visible');}btn.disabled=false;btn.textContent='Connect';}
function dismissNotif(triggerId,el){el.closest('.notif-item').remove();if(!notifBanner.querySelector('.notif-item'))notifBanner.classList.remove('visible');try{fetch(API+'/proactive/deliver/'+encodeURIComponent(triggerId),{method:'POST',signal:AbortSignal.timeout(2000)}).catch(function(){});}catch{}}
async function pollProactive(){try{const r=await fetch(API+'/proactive?n=5',{signal:AbortSignal.timeout(2000)});if(!r.ok)return;const d=await r.json();const events=d.events||[];if(!events.length){notifBanner.classList.remove('visible');return;}notifBanner.innerHTML='';events.forEach(function(e){const item=document.createElement('div');item.className='notif-item';item.innerHTML='<span>\u26a0\ufe0f '+esc(e.message)+'</span><button onclick="dismissNotif(\\''+esc(e.trigger_id)+'\\',this)" title="Dismiss">\u2715</button>';notifBanner.appendChild(item);});notifBanner.classList.add('visible');}catch{}}
ttsBtn.addEventListener('click',toggleTTS);
document.getElementById('composer').addEventListener('submit',function(event){event.preventDefault();send();});
function setMode(mode){state.mode=mode;document.querySelectorAll('.nav button').forEach(function(b){b.classList.toggle('active',b.getAttribute('data-mode')===mode);});const badge=document.getElementById('modeBadge');if(badge)badge.textContent=mode+' mode';renderQuick();}
document.body.addEventListener('click',function(event){const prompt=event.target.getAttribute('data-prompt');const mode=event.target.getAttribute('data-mode');const demoPrompt=event.target.getAttribute('data-demo');if(prompt)send(prompt);if(demoPrompt)send(demoPrompt);if(mode){setMode(mode);bubble('agent','<div class="card"><div class="label">PRISM</div>Switched to <strong>'+esc(mode)+'</strong> mode \u2014 try a prompt below.</div>');}});
(function(){const controller=new AbortController();const timer=setTimeout(function(){controller.abort();},1500);fetch(API+'/status',{signal:controller.signal}).then(function(response){if(response.status===401){clearTimeout(timer);showTokenBar();return null;}return response.json();}).then(function(data){if(!data)return;clearTimeout(timer);const ollamaStr=data.ollama?('Ollama: '+(data.ollama_model||'running')+' \u2713'):'Ollama: not running';statusText.textContent='connected \u00b7 '+ollamaStr;roleText.textContent=data.role||'connected';}).catch(function(){clearTimeout(timer);statusText.textContent='Server offline (demo mode)';roleText.textContent='demo';});setMode('General');try{fetch(API+'/perception/status',{signal:AbortSignal.timeout(1500)}).then(function(pr){if(pr.ok)return pr.json();}).then(function(p){if(!p)return;const el=document.getElementById('perception-status');if(p.active_channels&&p.active_channels.length){el.textContent='Sensing: '+p.active_channels.join(' \u00b7 ');if(p.summary&&p.summary!=='normal context')el.textContent+=' \u00b7 '+p.summary;}}).catch(function(){});}catch(e){}setInterval(pollProactive,30000);setTimeout(pollProactive,2000);setTimeout(async function(){let capStr='';let perceptStr='';try{const[capR,perR]=await Promise.all([fetch(API+'/device/capabilities',{signal:AbortSignal.timeout(1500)}),fetch(API+'/perception/status',{signal:AbortSignal.timeout(1500)}),]);if(capR.ok){const d=await capR.json();const cats=Object.keys(d.categories||{}).slice(0,5);if(cats.length)capStr='<br><span style="font-size:10.5px;color:var(--mu)">Device: '+cats.join(' \u00b7 ')+'</span>';}if(perR.ok){const p=await perR.json();if(p.summary&&p.summary!=='normal context')perceptStr='<br><span style="font-size:10.5px;color:var(--ac)">Context: '+p.summary+'</span>';}}catch{}bubble('agent',renderCard({type:'text',title:'PRISM',body:'Hello \ud83d\udc4b I\\'m PRISM \u2014 your decision intelligence.<br><br>I decide, plan, and execute \u2014 for any task, in any domain.'+capStr+perceptStr+'<br><br>What would you like to do today?',data:{}}));try{const cr=await fetch(API+'/ceremony/state',{signal:AbortSignal.timeout(3000)});if(cr.ok){const cd=await cr.json();if(cd&&cd.card)bubble('agent',renderCard(cd.card));}}catch{}try{const nr=await fetch(API+'/narrative/proactive',{signal:AbortSignal.timeout(3000)});if(nr.ok){const nd=await nr.json();if(nd&&nd.card)bubble('agent',renderCard(nd.card));}}catch{}},250);})();
</script>
</body>
</html>"""
